# GTM Territory Command Center

The command center app lives at the **repo root** for Vercel deployment.

- **Live:** [snowflake-qbr.vercel.app](https://snowflake-qbr.vercel.app)
- **Local:** Run `python3 -m http.server 8080` from the repo root, then open http://localhost:8080
